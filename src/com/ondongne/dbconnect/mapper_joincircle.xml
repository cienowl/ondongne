<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="joincircle">
	<insert id="joinCircleInsert" parameterType="com.ondongne.dto.DataTransferCircleJoin">
		insert into helper_circle values (#{join_email},#{join_postid});
	</insert>
	
	<select id="getJoinCircleAll" resultType="int" parameterType="com.ondongne.dto.DataTransferCircleJoin">
		select COUNT(*) from helper_circle where join_email=#{join_email} AND join_postid=#{join_postid};
	</select>
	
	<select id="getJoinCount" resultType="int" parameterType="int">
		select COUNT(*) from helper_circle where join_postid=#{join_postid};
	</select>
	
	<delete id="cancelJoinCircle" parameterType="com.ondongne.dto.DataTransferCircleJoin">
		delete from helper_circle where join_postid=#{join_postid} AND join_email=#{join_email};
	</delete>

</mapper>