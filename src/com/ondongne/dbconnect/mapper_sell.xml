<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sell">

	<insert id="sellPost" parameterType="com.ondongne.dto.DataTransferSell">
		insert into sell(
			email,
			id,
			title,
			price,
			region,
			description,
			is_parcel,
			is_active,
			pictures,
			tags,
			post_date
		)
		values(
			#{email},
			default,
			#{title},
			#{price},
			#{region},
			#{description},
			#{is_parcel},
			#{is_active},
			#{pictures},
			#{tags},
			now()
		)
	</insert>
	
	<select id="getSellPostAll" resultType="com.ondongne.dto.DataTransferSell">
		select * from sell
	</select>

</mapper>

