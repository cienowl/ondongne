<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="users">

	<insert id="insertBasicInfo" parameterType="com.ondongne.dto.DataTransferUsers">
		insert into users(email, password, joindate, name, phone)
		values(#{email}, #{password}, now(), #{name}, #{phone})
	</insert>

	<select id="checkEmail" resultType="String">
		select email from users where email=#{email}
	</select>

	<select id="checkSignin" resultType="com.ondongne.dto.DataTransferUsers" parameterType="String">
		select * from users where email=#{email}
	</select>

	<update id="userUpdate" parameterType="com.ondongne.dto.DataTransferUsers">
		update users set email=#{email}, password=#{password}, phone=#{phone}, birthday=#{birthday}, gender=#{gender}, name=#{name}, zipcode=#{zipcode}, address=#{address}, address_detail=#{address_detail}, region1=#{region1}, region2=#{region2}, region3=#{region3} where email=#{sessionEmail};
	</update>

	<delete id="userDelete" parameterType="String">
		delete from users where email=#{email};
	</delete>
	
	<update id="setAvatar" parameterType="com.ondongne.dto.DataTransferUsers">
		update users set profile_picture=#{profile_picture} where email=#{email}
	</update>
	
	<select id="findEmail" parameterType="com.ondongne.dto.DataTransferUsers" resultType="String">
		select email from users WHERE name=#{name} AND phone=#{phone};
	</select>
	
	<select id="userInfo" resultType="com.ondongne.dto.DataTransferUsers" parameterType="String">
		select * from users WHERE email=#{email};
	</select>
	
	<select id="getAvatar" parameterType="String" resultType="String">
		select profile_picture from users WHERE email=#{email};
	</select>
	
	<select id="checkPhone" parameterType="String" resultType="String">
		select phone from users where phone=#{phone};
	</select>
	
	<update id="deleteAvatar" parameterType="String">
		update users set profile_picture=null WHERE email=#{loginEmail};
	</update>
	
	<select id="selectUserAll" resultType="com.ondongne.dto.DataTransferUsers">
		select * from users order by joindate;
	</select>
	
	<delete id="adminDeleteUser" parameterType="String">
		delete from users where email=#{email};
	</delete>
	
	<select id="searchUserList" parameterType="String" resultType="com.ondongne.dto.DataTransferUsers">
		select * from users WHERE email LIKE CONCAT('%',#{searchWord},'%') OR name LIKE CONCAT('%',#{searchWord},'%') OR phone LIKE CONCAT('%',#{searchWord},'%') OR joindate LIKE CONCAT('%',#{searchWord},'%') order by joindate;
	</select>
	

</mapper>