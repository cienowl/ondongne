<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="users">

	<insert id="insertBasicInfo" parameterType="com.ondongne.dto.DataTransferUsers">
		insert into users(email, password, joindate, name, phone)
		values(#{email}, #{password}, now(), #{name}, #{phone})
	</insert>

	<select id="checkEmail" resultType="String">
		select email from users where email=#{email}
	</select>

	<select id="checkSignin" resultType="com.ondongne.dto.DataTransferUsers" >
		select email, password from users where email=#{email}
	</select>

	<update id="userUpdate" parameterType="com.ondongne.dto.DataTransferUsers">
		update users set email=#{email}, password=#{password}, phone=#{phone}, birthday=#{birthday}, gender=#{gender}, name=#{name}, zipcode=#{zipcode}, address=#{address}, address_detail=#{address_detail}, region1=#{region1}, region2=#{region2}, region3=#{region3} where email=#{sessionEmail};
	</update>

	<delete id="userDelete" parameterType="String">
		delete from users where email=#{email};
	</delete>
	
	<update id="setAvatar" parameterType="com.ondongne.dto.DataTransferUsers">
		update users set profile_picture=#{profile_picture} where email=#{email}
	</update>
	
	<select id="findEmail" parameterType="com.ondongne.dto.DataTransferUsers" resultType="String">
		select email from users WHERE name=#{name} AND phone=#{phone};
	</select>
	
	<select id="userInfo" resultType="com.ondongne.dto.DataTransferUsers" parameterType="String">
		select * from users WHERE email=#{email};
	</select>

</mapper>